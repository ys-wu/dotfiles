# Repo meta-files: live in the git repo but must NOT be applied to $HOME.
README.md
docs
Brewfile

# Safety guardrail: never manage these even if accidentally `chezmoi add`-ed.
# Secrets / credentials
.ssh/id_*
.ssh/*.pem
.ssh/known_hosts*
.claude.json
.claude.json.backup
.claude/.credentials*
.aws/credentials
.netrc
*.key
*.pem

# Noisy / machine-local Claude state (not worth backing up)
.claude/cache
.claude/sessions
.claude/session-env
.claude/projects
.claude/shell-snapshots
.claude/file-history
.claude/history.jsonl
.claude/backups
.claude/downloads
.claude/plugins

# tmux-resurrect's saved session snapshots (machine-local state). It writes to
# the XDG path unless ~/.tmux/resurrect already exists, so both are listed.
# NB: do NOT ignore .tmux/plugins: those are chezmoi externals, and an ignore
# entry would stop them from being applied.
.local/share/tmux
.tmux/resurrect

# History / caches / sockets
.zsh_history
.zsh_sessions
.zcompdump*
.node_repl_history
.viminfo
*.sock
.DS_Store
